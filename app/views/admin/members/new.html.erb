<% @page_title = "会員の新規登録" %>

<h1><%= @page_title %></h1>

<div class="toolbar"><%= link_to "会員一覧に戻る", admin_members_path %></div>

<%= form_for [:admin, @member] do |form| %>
    <%= render "shared/member_form", form: form %>
    <div><%= form.submit %></div>
<% end %>

